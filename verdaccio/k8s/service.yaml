apiVersion: v1
kind: Service
metadata:
  name: verdaccio
  namespace: krgeobuk-devops
  labels:
    app: verdaccio
spec:
  type: ClusterIP
  selector:
    app: verdaccio
  ports:
    - name: http
      port: 4873
      targetPort: 4873
      protocol: TCP
